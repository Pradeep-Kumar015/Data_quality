# Snowflake Snowpark
snowflake-snowpark-python>=1.15.0

# HTTP requests (for Teams alerts)
requests>=2.31.0

# Environment variable support
python-dotenv>=1.0.1

# Data processing
pandas>=2.2.0,<3

# Keep NumPy compatible with the MWAA PyArrow environment
numpy<2

# PyArrow compatible with NumPy 1.x
pyarrow>=14,<20